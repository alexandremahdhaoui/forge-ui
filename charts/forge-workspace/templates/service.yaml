apiVersion: v1
kind: Service
metadata:
  name: {{ include "forge-workspace.fullname" . }}
  labels:
    {{- include "forge-workspace.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: ssh
      protocol: TCP
      name: ssh
  selector:
    {{- include "forge-workspace.selectorLabels" . | nindent 4 }}
