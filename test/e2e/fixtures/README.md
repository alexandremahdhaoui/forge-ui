# Test Fixtures

These SSH keys are for E2E testing only. They are intentionally committed
to the repository for reproducible tests.

DO NOT use these keys in production.

- `test_user_ed25519_key` / `.pub` — test user private/public key
- `ssh_host_ed25519_key` / `.pub` — test host private/public key
- `authorized_keys` — authorized_keys file containing the test user public key
